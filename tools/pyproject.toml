[project]
name = "acs2-tools"
version = "0.0.0"
description = "Figure generation for the MPX experiments (kept out of baseline/ so the pinned pyalcs environment stays untouched)"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
]

[tool.uv]
package = false
