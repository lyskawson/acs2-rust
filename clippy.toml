disallowed-types = [
    { path = "std::collections::HashMap", reason = "iteration order is randomized per process; use BTreeMap or Vec so learning and output stay deterministic from the seed" },
    { path = "std::collections::HashSet", reason = "iteration order is randomized per process; use BTreeSet or Vec so learning and output stay deterministic from the seed" },
]

disallowed-methods = [
    { path = "f64::powi", reason = "precision documented as non-deterministic across platforms; multiply explicitly" },
    { path = "f64::powf", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::exp", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::exp2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::exp_m1", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::ln", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::ln_1p", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::log", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::log2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::log10", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::cbrt", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::hypot", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::sin", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::cos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::tan", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::sin_cos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::asin", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::acos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::atan", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::atan2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::sinh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::cosh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::tanh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::asinh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::acosh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f64::atanh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::powi", reason = "precision documented as non-deterministic across platforms; multiply explicitly" },
    { path = "f32::powf", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::exp", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::exp2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::exp_m1", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::ln", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::ln_1p", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::log", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::log2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::log10", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::cbrt", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::hypot", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::sin", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::cos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::tan", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::sin_cos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::asin", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::acos", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::atan", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::atan2", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::sinh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::cosh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::tanh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::asinh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::acosh", reason = "precision documented as non-deterministic across platforms" },
    { path = "f32::atanh", reason = "precision documented as non-deterministic across platforms" },
]
